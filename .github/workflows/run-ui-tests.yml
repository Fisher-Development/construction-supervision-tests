name: UI Tests with Allure Report

on:
  workflow_dispatch:
    inputs:
      test_url:
        description: 'URL для тестирования'
        required: true
        default: 'https://construction-supervision.alex-fisher-dev.ru/'
        type: string
      browser:
        description: 'Браузер для тестов'
        required: true
        default: 'chrome'
        type: choice
        options:
          - chrome
          - firefox
      test_path:
        description: 'Путь к тестам (оставьте пустым для всех тестов)'
        required: false
        default: 'tests/'
        type: string

jobs:
  test:
    runs-on: ubuntu-latest
    name: Запуск UI тестов
    
    # Даем разрешения для записи в Pages
    permissions:
      contents: write
      pages: write
      id-token: write
    
    steps:
      - name: Checkout кода
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      
      - name: Установка Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'
      
      - name: Установка зависимостей
        run: |
          python3 -m pip install --upgrade pip
          pip3 install -r requirements.txt
      
      - name: Установка Chrome
        uses: browser-actions/setup-chrome@latest
        with:
          chrome-version: stable
      
      - name: Установка ChromeDriver
        uses: nanasess/setup-chromedriver@v2
      
      - name: Проверка версий
        run: |
          python3 --version
          pip3 --version
          google-chrome --version
          chromedriver --version
      
      - name: Запуск тестов с Allure
        env:
          TEST_URL: ${{ inputs.test_url }}
        run: |
          pytest ${{ inputs.test_path }} \
            -v \
            --alluredir=allure-results \
            --clean-alluredir
        continue-on-error: true
      
      - name: Получить историю Allure (если существует)
        if: always()
        continue-on-error: true
        run: |
          if git ls-remote --exit-code --heads origin gh-pages; then
            echo "Ветка gh-pages существует, получаем историю..."
            git fetch origin gh-pages
            git checkout gh-pages
            if [ -d "allure-history" ]; then
              cp -r allure-history /tmp/allure-history
            fi
            git checkout -
            if [ -d "/tmp/allure-history" ]; then
              cp -r /tmp/allure-history allure-results/history
            fi
          else
            echo "Ветка gh-pages не существует, это первый запуск"
          fi
      
      - name: Сгенерировать Allure отчет
        uses: simple-elf/allure-report-action@master
        if: always()
        with:
          allure_results: allure-results
          allure_history: allure-history
          keep_reports: 20
      
      - name: Создать .nojekyll файл
        if: always()
        run: |
          touch allure-history/.nojekyll
      
      - name: Опубликовать отчет на GitHub Pages
        if: always()
        run: |
          cd allure-history
          git init
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add .
          git commit -m "Deploy Allure report - run #${{ github.run_number }}"
          git push --force https://x-access-token:${{ secrets.GITHUB_TOKEN }}@github.com/${{ github.repository }}.git HEAD:gh-pages
      
      - name: Получить имя репозитория
        if: always()
        id: repo_name
        run: echo "REPO_NAME=${GITHUB_REPOSITORY#*/}" >> $GITHUB_OUTPUT
      
      - name: Добавить ссылку на отчет в Summary
        if: always()
        run: |
          echo "## 📊 Allure отчет опубликован! ✅" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "Отчет будет доступен через 1-2 минуты по ссылке:" >> $GITHUB_STEP_SUMMARY
          echo "### 🔗 https://${{ github.repository_owner }}.github.io/${{ steps.repo_name.outputs.REPO_NAME }}/" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "> 💡 **Первый запуск?** Включите GitHub Pages:" >> $GITHUB_STEP_SUMMARY
          echo "> 1. Перейдите в **Settings → Pages**" >> $GITHUB_STEP_SUMMARY
          echo "> 2. Source: **Deploy from a branch**" >> $GITHUB_STEP_SUMMARY
          echo "> 3. Branch: **gh-pages** → **/ (root)**" >> $GITHUB_STEP_SUMMARY
          echo "> 4. Нажмите **Save**" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### 📋 Параметры запуска:" >> $GITHUB_STEP_SUMMARY
          echo "- 🌐 URL: ${{ inputs.test_url }}" >> $GITHUB_STEP_SUMMARY
          echo "- 🌍 Браузер: ${{ inputs.browser }}" >> $GITHUB_STEP_SUMMARY
          echo "- 📁 Тесты: ${{ inputs.test_path }}" >> $GITHUB_STEP_SUMMARY
          echo "- 🔢 Запуск №: ${{ github.run_number }}" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### 📦 Артефакты:" >> $GITHUB_STEP_SUMMARY
          echo "Результаты также сохранены в артефактах (см. выше)" >> $GITHUB_STEP_SUMMARY
      
      - name: Загрузить Allure results как артефакт
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: allure-results-${{ github.run_number }}
          path: allure-results
          retention-days: 30
      
      - name: Загрузить Allure report как артефакт
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: allure-report-${{ github.run_number }}
          path: allure-history
          retention-days: 30
